@model List<WebSites.Panles.Models.Authorize.PermistionModel>
@using System.Linq
@{
    ViewData["Title"] = "GetPermisions";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-12 col-md-12 col-sm-12">
    <div class="card">

        <div class="card-body">
            @if (Model != null)
            {
                <div class="row">
                    <div class="col-md-10 col-lg-8 col-xl-6 mx-auto d-block">
                        <div class="card card-body pd-20 pd-md-40 border shadow-none">
                            <h5 class="card-title mg-b-20">ویرایش دسترسی ها: @ViewBag.AppName - @ViewBag.RoleName</h5>
                            <hr class="bg-danger border-danger" />
                            <form action="/Admin/Role/CreatePermisions" method="post" data-parsley-validate="">
                                <input type="hidden" value="@ViewBag.RoleId" name="roleId" />
                                <div class="row">
                                    <div class="form-group">
                                        <!-- col -->
                                        <div class="table-responsive" tabindex="1" style="overflow:auto; outline: none;">
                                            <table class="table table-striped table-hover mg-b-0 text-md-nowrap">
                                                @foreach (var item in Model.Where(p => p.ParentId == 0 || p.ParentId == null).ToList())
                                                {
                                                    var items = Model.Where(p => p.ParentId == item.PageId).ToList();

                                                    <tr class="bg-dark">
                                                        <td class="text-white-50">@item.DisplayName</td>
                                                        <td></td>
                                                        <td></td>
                                                    </tr>
                                                    foreach (var subitem in items)
                                                    {
                                                        <tr>
                                                            <td class="text-left">
                                                                <input type="checkbox" id="@subitem.PageId" checked="@subitem.IsAccess"/>
                                                            </td>
                                                            <td>@subitem.DisplayName</td>
                                                            <td class="text-left">
                                                                @if (subitem.IsAccess)
                                                                {
                                                                    <a class="btn btn-danger tx-10 " href="#" onclick="delPermision('@subitem.PageId','@subitem.RoleId')"> حذف دسترسی </a>
                                                                }
                                                                else
                                                                {
                                                                    <a class="btn btn-success tx-10" href="#" onclick="addPermision('@subitem.PageId','@subitem.RoleId')">تخصیص دسترسی</a>
                                                                }
                                                            </td>
                                                        </tr>
                                                    }
                                                }
                                            </table>
                                        </div>
                                    </div>
                                </div>
                                <hr class="bg-danger border-danger" />
                                <div class="form-group">
                                    <input type="submit" value="ویرایش اطلاعات" class="btn btn-danger btn-block" />
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            }
            else
            {
                <p class="text-danger">موردی برای ویرایش وجود ندارد</p>
            }
        </div>

        <div class="card-footer">
            <a href="/Admin/Role/AppRoles?appId=@ViewBag.AppId">برگشت</a>
        </div>

    </div>
</div>

@section Scripts
{
    <script>

        function addPermision(pageId, roleId) {
            let postData = {
                'pageId': pageId,
                'roleId': roleId
            };

            $.ajax({
                //contentType: 'application/x-www-form-urlencoded',
                dataType: 'json',
                type: "POST",
                url: "/Admin/Role/AddPermision",
                data: postData,
                beforeSend: function () {
                    showBehsamLoading();
                },
                complete: function () {
                    hideBehsamLoading();
                },
                success: function (data) {
                    if (data.isSuccess == true) {
                        location.reload();
                    }
                },
                error: function (request, status, error) {
                    hideBehsamLoading();
                }

            });
        }

        function delPermision(pageId, roleId) {
            let postData = {
                'pageId': pageId,
                'roleId': roleId
            };

            $.ajax({
                //contentType: 'application/x-www-form-urlencoded',
                dataType: 'json',
                type: "POST",
                url: "/Admin/Role/RemovePermision",
                data: postData,
                beforeSend: function () {
                    showBehsamLoading();
                },
                complete: function () {
                    hideBehsamLoading();
                },
                success: function (data) {
                    if (data.isSuccess == true) {
                        location.reload();
                    }
                },
                error: function (request, status, error) {
                    hideBehsamLoading();
                }

            });
        }

    </script>
}
